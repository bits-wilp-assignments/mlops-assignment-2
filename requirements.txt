# MLOps Assignment 2 - Main Requirements
# This file contains core dependencies for the entire project
# For module-specific dependencies, see:
#   - training/requirements.txt (Training pipeline)
#   - serving/requirements.txt (API serving)
#   - dev-requirements.txt (Development tools)

# Deep Learning Framework
# Platform-specific installation:
# --- MAC SETUP ---
# Installs base engine + Apple's GPU bridge
tensorflow==2.18.0; sys_platform == 'darwin'
tensorflow-metal==1.2.0; sys_platform == 'darwin'

# --- LINUX/WINDOWS SETUP ---
# Installs base engine + NVIDIA libraries (CUDA/cuDNN) bundled in
tensorflow[and-cuda]==2.18.0; sys_platform != 'darwin'

# MLOps Tools
mlflow==3.9.0
dvc==3.66.1
dvc-gdrive==3.0.1

# Data Science & ML
numpy==2.0.2
scikit-learn==1.8.0
pillow==12.1.1
matplotlib==3.10.8
seaborn==0.13.2

# API Framework
fastapi==0.129.0
uvicorn==0.41.0
python-multipart==0.0.22

# Monitoring
prometheus-client==0.24.1

# HTTP Client
requests==2.32.5

# Testing
pytest==9.0.2
